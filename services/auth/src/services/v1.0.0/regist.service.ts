// Interfaces
import { Tokens } from '@ts/interfaces/tokens.interface'

export const registService = (): Tokens => {
  const refreshToken = ''
  const accessToken = ''

  return {
    refreshToken,
    accessToken,
  }
}
